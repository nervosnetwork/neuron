{
  "name": "@nervosnetwork/neuron-shared",
  "version": "0.110.1",
  "private": true,
  "main": "./dist/main.js",
  "scripts": {
    "test": "cross-env NODE_ENV=test jest --runInBand --collect-coverage --forceExit",
    "test:watch": "cross-env NODE_ENV=test jest --watch",
    "build": "tsc",
    "start": "tsc -w --preserveWatchOutput",
    "watch": "tsc -w"
  },
  "devDependencies": {
    "dotenv-expand": "10.0.0"
  }
}
